# Only needed if the backend isn't reachable at the relative /api path
# (e.g. it's deployed on a different domain). Leave unset for local dev,
# the Vite proxy in vite.config.ts handles it for you.
# VITE_API_BASE_URL=http://localhost:3001/api
